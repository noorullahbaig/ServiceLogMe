# Explicitly enable the browser-local workspace for a local production-build check.
# This is NOT an authenticated or shared deployment. Keep unset on hosted deployments.
# NEXT_PUBLIC_LOCAL_WORKSPACE=1
